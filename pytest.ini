[pytest]
# Keep on-camera test output clean: silence the Starlette TestClient/httpx
# deprecation notice (cosmetic — the demo records `pytest -q` on screen).
filterwarnings =
    ignore::DeprecationWarning:starlette.*
    ignore:.*httpx.*starlette.testclient.*:DeprecationWarning
